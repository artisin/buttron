-mediaQ/mixin/density
Should create a density q like rupture except without all the prefixin since to make that work I would have to create some janky hack but then again they should be using a prefixer so fuck em. 